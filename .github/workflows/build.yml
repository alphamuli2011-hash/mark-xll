name: Build Mark-XLVI EXE

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Upgrade pip
        run: python -m pip install --upgrade pip setuptools wheel

      - name: Install core dependencies
        run: |
          pip install sounddevice pillow requests beautifulsoup4 numpy psutil send2trash qrcode[pil]

      - name: Install Google AI dependencies
        run: |
          pip install google-genai google-generativeai

      - name: Install web and media dependencies
        run: |
          pip install duckduckgo-search youtube-transcript-api python-pptx opencv-python mss

      - name: Install Windows-specific dependencies
        run: |
          pip install comtypes pycaw win10toast pywinauto pyautogui pyperclip pygetwindow

      - name: Install server dependencies
        run: |
          pip install fastapi "uvicorn[standard]" cryptography python-multipart

      - name: Install Playwright
        run: |
          pip install playwright
          playwright install chromium --with-deps

      - name: Install PyInstaller
        run: pip install pyinstaller

      - name: Build EXE with PyInstaller
        run: |
          pyinstaller `
            --noconfirm `
            --onedir `
            --windowed `
            --name "Mark-XLVI" `
            --add-data "config;config" `
            --add-data "core/prompt.txt;core" `
            --add-data "dashboard/static;dashboard/static" `
            --hidden-import "sounddevice" `
            --hidden-import "google.genai" `
            --hidden-import "google.generativeai" `
            --hidden-import "PIL" `
            --hidden-import "cv2" `
            --hidden-import "pyautogui" `
            --hidden-import "pyperclip" `
            --hidden-import "pygetwindow" `
            --hidden-import "mss" `
            --hidden-import "psutil" `
            --hidden-import "send2trash" `
            --hidden-import "fastapi" `
            --hidden-import "uvicorn" `
            --hidden-import "cryptography" `
            --hidden-import "comtypes" `
            --hidden-import "pycaw" `
            --hidden-import "win10toast" `
            --hidden-import "pywinauto" `
            --collect-all "google.genai" `
            --collect-all "google.generativeai" `
            --collect-all "uvicorn" `
            --collect-all "fastapi" `
            main.py

      - name: Upload EXE as artifact
        uses: actions/upload-artifact@v4
        with:
          name: Mark-XLVI-Windows
          path: dist/Mark-XLVI/
          retention-days: 30
